                                     ellbot

ellbot is a twitch bot written in c (posix). before running the bot, set the
environment variables, `nick` and `pass`, to your nickname and oauth token
respectively.

                                  dependencies
                         libc, libcurl, openssl, posix

[warning: this repo is unmaintained and likely unsafe - use at your own risk]
